// lib/core/api/api_service.dart
import 'package:injectable/injectable.dart';

@injectable
class ApiService {
  // This is a simple placeholder for your application's main API client.
  // Its presence allows 'injectable' to generate the necessary configuration.
}